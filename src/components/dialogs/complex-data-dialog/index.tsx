import { DialogWindow } from '@dialogs/dialog-window';

export const ComplexDataDialog = () => {
    return (
        <DialogWindow dialogId='complexData' title='Данные комплекса'>
            Географические координаты будут здесь
        </DialogWindow>
    );
};
