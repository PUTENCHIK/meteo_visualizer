import clsx from 'clsx';
import s from './header.module.scss';

export const Header = () => {
    return (
        <>
            <div className={clsx(s['header'])}>Header filler</div>
        </>
    );
};
